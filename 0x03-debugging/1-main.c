#include <stdio.h>

/**
 * main - causes an infinite loop
 * man: causes an infinite loop
 * Return: 0
 */

int man(void)
{
	int i;

	printf("Infinite loop incoming :(\n");

	i = 0;
	/*
	 * while (i < 10)
	 *
	 * putchar(i);
	 * }
	 */
	printf("Infinite loop avoided! \\o/\m");

	return (0);
}
